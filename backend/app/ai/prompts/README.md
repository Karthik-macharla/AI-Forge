# Prompt templates go here as .txt or .yaml files.
# Never define prompt strings inline in chain files — reference them from here.
#
# Example:
#   chat_system.txt    — system prompt for the main chat chain
#   rag_system.txt     — system prompt for RAG responses
#   sql_agent.txt      — system prompt for NL-to-SQL agent
